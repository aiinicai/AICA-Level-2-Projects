"""Core processing engines for PDF Office Utility.

Every module in this package is pure/back-end logic with no Qt/GUI
dependencies, so it can be unit tested and reused (e.g. from a future CLI
or folder-watcher service) independently of the desktop UI.
"""
