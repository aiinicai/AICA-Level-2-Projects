# Minimum versions, not exact pins - lets pip pick whatever release is
# actually compatible with the Python you have installed. Pinning exact
# versions here would go stale as new Python releases come out (this is
# what caused the "No matching distribution found for pyinstaller==6.10.0"
# error - 6.10.0 doesn't support newer Python versions; newer PyInstaller
# releases do).
flask>=3.0
playwright>=1.47
pywebview>=5.3
openpyxl>=3.1
pyinstaller>=6.10
