# Equity Research Framework — pinned dependencies
# Target: Python 3.13.x, 64-bit, Windows 10/11
#
# NOTE ON VERSIONING:
# Versions below were current-stable and Python 3.13-compatible as of the
# last verification. Before installing on a new machine, confirm with:
#   pip index versions <package>
# and adjust upward if a listed version has since been yanked. Do not
# blindly `pip install -U` across this file without re-running the test
# suite afterward.

# --- Core data / numerics ---
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1

# --- Structured models / config ---
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Spreadsheet I/O ---
openpyxl==3.1.5

# --- Charting ---
matplotlib==3.9.3
plotly==5.24.1

# --- Document export ---
python-docx==1.1.2

# --- Market data ---
# yfinance MUST be a recent version (confirmed working at 1.5.2, live-tested
# 2026-08-12). The originally-pinned 0.2.51 fails against Yahoo Finance's
# current backend with an empty-response error (Yahoo added a crumb/cookie
# anti-bot requirement older yfinance versions can't satisfy) — confirmed
# via a real live test, not a guess. If you upgrade further, re-run
# scripts/verify_yfinance_live.py to confirm it still works before assuming so.
yfinance==1.5.2
requests==2.32.3

# --- PDF / document intelligence ---
pymupdf==1.25.1

# --- AI layer ---
openai==1.59.6
google-genai==2.18.0

# --- UI ---
streamlit==1.41.1

# --- Testing / quality ---
pytest==8.3.4
pytest-cov==6.0.0
pytest-mock==3.14.0

# --- Type checking / lint (optional, dev use) ---
mypy==1.13.0
ruff==0.8.4
