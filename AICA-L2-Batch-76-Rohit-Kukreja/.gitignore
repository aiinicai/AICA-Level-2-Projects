# Secrets and local environment
.env
.venv/
venv/

# Client data — never commit. Contains unissued audit opinions and financials.
data/
*.db
*.db-wal
*.db-shm

# Caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Build output. `dist/` holds the 31 MB packaged .exe and `build/` PyInstaller's
# intermediates -- both are produced from the source in this repository and
# have no business being in it.
build/
dist/
gate_a_pack/
*.spec.bak

# Runtime logs
*.log

# Editor/agent tooling, not part of the project
.claude/
