# AUTHORED from the firm's own report format.
#
# NOT PRODUCED BY THE SCAFFOLDER. `scripts/scaffold_clauses.py` carries a
# hand-written clause table rather than reading the approved register, and
# this row was never transcribed into it — so the document simply had no
# such paragraph. tests/test_register_reconciliation.py now fails if any
# approved row is missing.

id: iar.om
document: auditors_report
order: 110
number: ""
title: "Other Matter"
clause_ref: "SA 706 (Revised) para 10"
effective_from: "2014-04-01"
effective_to: null
render_as: section

needs_review: false

input:
  key: iar.om
  label: "Other Matter paragraph"
  datatype: select
  carry_forward: prompt
  mandatory: true
  options:
    - { value: "none", label: "None" }
    - { value: "om", label: "Other Matter — describe" }

variants:
  - when: "value == 'none'"
    body: >
      There is no Other Matter paragraph, so this section is not printed.
    omit: true

  # AUTHORED — the firm's format has no Other Matter section.
  - when: "value == 'om'"
    body: >
      Other Matter
    requires_narrative: true
