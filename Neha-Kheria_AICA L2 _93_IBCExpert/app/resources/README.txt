Place ONLY the owner's public licence verification key here as:
  licence_public_key.hex

Never place owner_private_key.ibckey or any private signing key inside app/resources or the customer package.
The same public key may alternatively be installed in the user's protected IBC Expert config directory.
