# Cryptography: Argon2id KDF + AES-256-GCM AEAD (via OpenSSL bindings)
argon2-cffi>=23.1.0
cryptography>=42.0.0

# Face detection (Haar cascade) + face recognition (LBPH), both built into
# OpenCV's contrib module. NOTE: this must be opencv-contrib-python, not
# plain opencv-python — the base package does not include cv2.face. Do not
# install both packages in the same environment (they conflict); if you
# have opencv-python installed, remove it first:
#   pip uninstall opencv-python -y
opencv-contrib-python>=4.9.0

# Camera capture + image handling
numpy>=1.24.0
Pillow>=10.0.0
