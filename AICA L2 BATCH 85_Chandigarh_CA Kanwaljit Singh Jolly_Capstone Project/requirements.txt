# Task Checker runtime dependencies.
# Trimmed to what the live app actually imports. numpy is pulled in transitively
# by pandas; PDF text extraction uses PyMuPDF (fitz).

# Web framework
Flask==3.0.0
Flask-CORS==4.0.0
werkzeug==3.0.1
gunicorn==21.2.0

# Config / HTTP
python-dotenv==1.0.0
requests==2.31.0

# File processing
pandas==2.1.4
openpyxl==3.1.2
python-docx==1.1.0
PyMuPDF==1.24.9

# AI
openai==1.12.0
openai-codex==0.147.0
# Optional: only needed when the panel uses a claude-* juror/critic model
# (AI_JURY_MODELS / AI_CRITIC_MODEL). OpenAI-only deployments can omit it.
anthropic==0.39.0

# Auth / DB (Supabase)
supabase==2.4.5
gotrue==2.9.0
httpx==0.27.0

# Encrypt tenant-owned Codex credentials at rest
cryptography==46.0.1
