PraNam E-Invoice Converter 2.2.5

UI/workflow fixes:
- Review & Correct opens as a maximize-first workspace on Windows and uses a larger fallback size.
- Added horizontal scrollbar to line-item grid so UQC and later columns remain reachable.
- Generate Files action is available in the main window after corrections pass validation.
- Main screen retains one authoritative working invoice state after the correction window closes.
- Main action area uses a two-row responsive layout to prevent button clipping.
- Correction footer keeps Re-check and Generate Files visible.
