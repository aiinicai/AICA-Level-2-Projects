# Runtime dependencies for the Personal Finance & Debt Impact Calculator.
# The app also self-installs these on first run when launched with plain Python.
streamlit>=1.36
pandas>=2.2
numpy>=1.26
plotly>=5.20
xlsxwriter>=3.2
openpyxl>=3.1

# Only needed to rebuild the standalone Windows application:
#   pip install pyinstaller
#   pyinstaller FinancePlanner.spec --noconfirm
