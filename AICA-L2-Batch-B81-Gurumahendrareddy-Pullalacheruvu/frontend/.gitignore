node_modules
.vite
*.local

# NOT dist. The backend serves frontend/dist directly, and the README promises
# the project runs with no Node and no npm install. Excluding it would ship a
# repository whose own instructions cannot work.
