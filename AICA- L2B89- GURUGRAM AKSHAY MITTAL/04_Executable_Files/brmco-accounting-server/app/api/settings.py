"""/api/v1/settings — centrally managed configuration pushed to Local Hosts (Phase 5)."""
from app.api._placeholder import placeholder_router

router = placeholder_router("settings", 5, "Central configuration (tax ledger mappings, template versions).")
