"""/api/v1/clients — CA-office client registry (Phase 5)."""
from app.api._placeholder import placeholder_router

router = placeholder_router("clients", 5, "Client companies, GSTINs and their Local Host installations.")
