streamlit
pandas
openpyxl
pymupdf
pytesseract
Pillow

# Invoice Check's free/offline OCR fallback (pytesseract above) is only a Python
# wrapper - it needs the Tesseract OCR engine itself installed separately:
#   Windows : https://github.com/UB-Mannheim/tesseract/wiki
#   macOS   : brew install tesseract
#   Linux   : sudo apt install tesseract-ocr
# Not required if you're using the AI reading path instead (see README section 5).
# On Windows, the app auto-detects the installer's default location
# (C:\Program Files\Tesseract-OCR\tesseract.exe) even if it isn't on PATH -
# if you installed it somewhere else, set a TESSERACT_CMD environment variable
# to the full path of tesseract.exe.

# Optional, for the AI features (plain-language explanation, and AI-based invoice
# reading when ANTHROPIC_API_KEY is set) - not required for the rest of the tool:
#   pip install anthropic
