# Point this at any Postgres you can reach. The capstone uses its own database
# so it can never touch the main MGSG one.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mgsg_capstone"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=5100
