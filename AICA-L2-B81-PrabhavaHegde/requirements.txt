# Runtime
PyYAML>=6.0
python-docx>=1.1.0
fastapi>=0.110
uvicorn>=0.29
# Required by the evidence upload endpoint.
python-multipart>=0.0.9

# Build and checks only
pyinstaller>=6.0
httpx>=0.27          # FastAPI's TestClient, used by checks/smoke_check.py

# AI drafting needs no SDK: Gemini is called over HTTPS with the standard library.
