# FinSight — approved Stage 1 package list (Blueprint Section L).
# Do not add a new dependency without flagging it first — several
# candidates (e.g. Flask-SQLAlchemy) were deliberately NOT approved.

Flask>=3.0,<4.0
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
reportlab>=4.1,<5.0
pydantic>=2.6,<3.0
waitress>=3.0,<4.0
python-dotenv>=1.0,<2.0
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# Dev/build-only — needed to build the packaged EXE (Stage 17), never
# imported by the application itself and never shipped inside the
# built package's own dependency set. Install this only in the build
# environment (see documentation/stage17_exe_packaging.md, Build
# Requirements) — a real Windows machine with network access, not
# this development sandbox (see that document's Known Limitations for
# why PyInstaller could not be installed or run here).
# PyInstaller>=6.5,<7.0

# Only imported if/when AI is enabled (Blueprint Ambiguity #7) — isolated
# to app/ai/providers/external_api_provider.py:
# requests>=2.31,<3.0
