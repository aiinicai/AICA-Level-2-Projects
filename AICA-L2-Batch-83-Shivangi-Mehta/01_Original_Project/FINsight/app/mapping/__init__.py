"""
Column-mapping package — populated in Stage 6/7.

column_mapper.py (fuzzy-match + synonym lookup) and synonyms.json are
added here. Per Blueprint Section 8: never auto-apply a suggested
mapping — is_user_confirmed must be explicitly set before a mapping is
used by any downstream rule.
"""
