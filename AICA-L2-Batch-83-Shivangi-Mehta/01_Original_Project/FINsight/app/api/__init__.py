"""
API blueprints package.

One blueprint module per functional area, matching the approved folder
structure (Blueprint Section C) and the approved top-level navigation
(Blueprint Section 8). Every blueprint in this Stage 2 skeleton exposes
only a placeholder route — real routes/logic arrive in the stage that
owns that area (see each file's docstring for its stage number).
"""
