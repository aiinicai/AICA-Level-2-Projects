# Live server configuration for the Fixed Asset Register.
# Created automatically so the app can be safely shared with other users on
# your office network via run_far.bat. You generally do not need to edit
# this file by hand.
#
# To temporarily see full error details while troubleshooting on your own
# machine only, change DJANGO_DEBUG below to True, then change it back to
# False before anyone else uses the app again — with DEBUG on, error pages
# show internal file paths and settings to anyone who hits an error, which
# is fine for you alone but not for a shared network app.

# --- Core ---------------------------------------------------------------
DJANGO_SECRET_KEY=hxy#ej@cx!7$2xu#!w!#9o$g5gkdwjj6tt8in#-%%%9ga)u^_8
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=*
DJANGO_CSRF_TRUSTED_ORIGINS=

# --- Database -------------------------------------------------------------
# Leave DJANGO_DB_ENGINE unset to keep using the bundled SQLite file
# (db.sqlite3) — this is what run_far.bat is set up for. Only change this if
# you later move to a proper PostgreSQL server.
DJANGO_DB_ENGINE=
DJANGO_DB_NAME=far_db
DJANGO_DB_USER=far_user
DJANGO_DB_PASSWORD=
DJANGO_DB_HOST=localhost
DJANGO_DB_PORT=5432

# --- Security (set True only once this is served over HTTPS) --------------
DJANGO_SECURE_COOKIES=False

# --- FAR policy -----------------------------------------------------------
FAR_COMPANY_NAME=Sample Manufacturing Industries Ltd.
# FAR_QR_BASE_URL is intentionally NOT set here — run_far.bat detects this
# computer's current network address each time it starts and sets it
# automatically, so QR codes always point somewhere reachable from other
# devices on the network even if your network address changes later.
