# Bank Guarantee Lifecycle Monitoring System — runtime dependencies
# Everything else used by the engine (smtplib, email, zoneinfo, tkinter,
# argparse, uuid) ships with the Python standard library.

gspread>=6.0.0
google-auth>=2.30.0

# Optional — only needed to build the standalone executables:
# pyinstaller>=6.0.0
