import TrendChart from './TrendChart';

// Margin trends (%) share the same multi-line-over-periods shape as TrendChart.
export default function MarginTrendChart(props) {
  return <TrendChart {...props} />;
}
