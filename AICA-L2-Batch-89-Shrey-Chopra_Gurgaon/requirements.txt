Flask>=3.0
pandas>=2.0
openpyxl>=3.1
reportlab>=4.0
python-docx>=1.1
matplotlib>=3.7

# Optional — only needed if you turn on "Polish commentary wording with AI"
# in the app. The app works fully without it (falls back to rule-based text).
openai>=1.30
